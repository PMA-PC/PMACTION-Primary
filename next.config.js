/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Add any other Next.js specific configurations here if needed later
  // For now, we keep it minimal
}

module.exports = nextConfig